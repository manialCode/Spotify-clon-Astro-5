---
import LeftRow from "@/icons/LeftRow.astro";

interface Props {
  aHref?: string;
}

const { aHref } = Astro.props;
---

<a
  id="Back-to-home-Button"
  href={aHref ?? "/"}
  class="text-zinc-200 group group-hover:text-zinc-400 inline player:hidden"
>
  <LeftRow />
</a>
